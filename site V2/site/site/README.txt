Installer npm ou bun (ou les deux)
npm install astro ou bun install

Python :
FastAPI (pip install fastapi uvicorn)
pip install HTTP

(S'il ya des erreurs, il faut installer node JS et voir)

Pour lancer le site, ouvrir deux terminaux à la racine du répertoire du site et ecrire
uvicorn api:app --reload (sur le premier terminal)
npm run dev (ou bun dev) sur l'autre terminal

ouvrir http://localhost:4321/ sur un navigateur

tester le site